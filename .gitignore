# =============================================================================
# BYH (SelectionAssistant) — .gitignore
# =============================================================================
# 策略说明（见 docs/git-workflow.md）：
#   - bin/ obj/ 是构建中间产物，9 个 csproj 共 ~1.8 GB，必排除
#   - 编译产物（BYH.exe + native DLL）不进 git，用 GitHub Releases 分发
#   - artifacts/qa/ 排除（QA 截图 ~126MB，开发期产物，精选的已复制到
#     docs/screenshots/ 作为公开展示）
#   - 用户运行时数据在仓库外：%LOCALAPPDATA%\BYH\，不在此文件管辖
# =============================================================================

## --- .NET 构建产物 ---
**/bin/
**/obj/
project.assets.json
project.nuget.cache
*.nuget.dgspec.json
*.nuget.g.props
*.nuget.g.targets
*.csproj.nuget.*

## --- NuGet ---
*.nupkg
*.snupkg
.nuget/

## --- 测试 / 覆盖率 ---
TestResults/
[Tt]est[Rr]esult*/
*.coverage
*.coveragexml
*.trx
*.trx.binlog

## --- IDE / 编辑器 ---
.vs/
.vscode/
.idea/
*.user
*.suo
*.userprefs
*.swp
*~

## --- Python QA tooling ---
__pycache__/
*.py[cod]

## --- 操作系统垃圾 ---
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

## --- 日志 ---
*.log
diag.log

## --- 散落备份（handoff / reqbase 流程中常见） ---
*.bak
*.bak-*
*.diag
*.experiment-bak
*.orig
*.rej

## --- 编译产物（不进仓库，用 GitHub Releases 分发；见 docs/git-workflow.md） ---
artifacts/publish/
artifacts/__diag/

## --- 严禁纳入的本地密钥文件（防御性 —— 真实密钥在 %LOCALAPPDATA%\BYH\secrets\） ---
secrets.json
secrets.yaml
*.secret
*.apikey
.env
.env.*

## --- QA 截图（开发期产物，~126MB，精选的已在 docs/screenshots/） ---
artifacts/qa/

## --- 分享包（本地打包产物，含 exe + 文档，不进版本控制） ---
dist/

## --- 第三方工具（本地运行依赖，不进版本控制） ---
## tools/LibreHardwareMonitor/ 是解压的 Libre Hardware Monitor 程序（19MB，45 文件）。
## BYH 功耗监控走 HTTP 轮询用户自装的 LHM Web Server，代码不引用这些二进制。
## 用户自行从 https://github.com/LibreHardwareMonitor/LibreHardwareMonitor 下载安装。
tools/LibreHardwareMonitor/

## --- reqbase / 多 agent 协作过程产物（仓库根目录散落文件） ---
## 这些是开发过程文件，对公众无价值；保留在本地但不纳入版本控制。
output/
requirements/
tasks/
.reqbase/
index.yaml
preferences.yaml
project.md
enabled-modules.yaml
probe-region.png
