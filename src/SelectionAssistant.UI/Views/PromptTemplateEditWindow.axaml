<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i18n="clr-namespace:SelectionAssistant.Core.I18n;assembly=SelectionAssistant.Core"
        x:Class="SelectionAssistant.UI.Views.PromptTemplateEditWindow"
        Title="{x:Static i18n:Strings.PromptEdit_Title}"
        Width="520"
        Height="380"
        MinWidth="420"
        MinHeight="300"
        CanResize="True"
        WindowStartupLocation="CenterOwner"
        Topmost="True"
        KeyDown="OnWindowKeyDown">
  <Border Padding="16">
    <Grid RowDefinitions="Auto,Auto,Auto,*,Auto,Auto,Auto" RowSpacing="10">

      <StackPanel Grid.Row="0" Spacing="4">
        <TextBlock x:Name="TitleText"
                   Text="{x:Static i18n:Strings.PromptEdit_Heading}"
                   Classes="DisplayTitle"
                   FontSize="{DynamicResource ByhFontSizeHeading}" />
        <TextBlock x:Name="SubtitleText"
                   Text="{x:Static i18n:Strings.PromptEdit_Subtitle}"
                   Classes="Secondary"
                   FontSize="{DynamicResource ByhFontSizeBody}"
                   TextWrapping="Wrap" />
      </StackPanel>

      <StackPanel Grid.Row="1" x:Name="NamePanel" Spacing="6" IsVisible="False">
        <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
          <StackPanel Grid.Column="0" Spacing="4">
            <TextBlock Text="{x:Static i18n:Strings.PromptEdit_NameZh}"
                       Classes="Muted"
                       FontSize="{DynamicResource ByhFontSizeBody}" />
            <TextBox x:Name="NameZhInput"
                     Classes="Bordered"
                     PlaceholderText="{x:Static i18n:Strings.PromptEdit_NameZhPlaceholder}"
                     FontSize="{DynamicResource ByhFontSizeBodyLarge}" />
          </StackPanel>
          <StackPanel Grid.Column="1" Spacing="4">
            <TextBlock Text="{x:Static i18n:Strings.PromptEdit_NameEn}"
                       Classes="Muted"
                       FontSize="{DynamicResource ByhFontSizeBody}" />
            <TextBox x:Name="NameEnInput"
                     Classes="Bordered"
                     PlaceholderText="{x:Static i18n:Strings.PromptEdit_NameEnPlaceholder}"
                     FontSize="{DynamicResource ByhFontSizeBodyLarge}" />
          </StackPanel>
        </Grid>
        <TextBlock x:Name="NameHintText"
                   Classes="Secondary"
                   FontSize="{DynamicResource ByhFontSizeBodySmall}"
                   TextWrapping="Wrap" />
      </StackPanel>

      <TextBlock Grid.Row="2"
                 x:Name="DefaultHintText"
                 Classes="Muted"
                 FontSize="{DynamicResource ByhFontSizeBodySmall}"
                 TextWrapping="Wrap" />

      <TextBox Grid.Row="3"
               x:Name="PromptInput"
               Classes="Bordered"
               AcceptsReturn="True"
               TextWrapping="Wrap"
               FontSize="{DynamicResource ByhFontSizeBodyLarge}" />

      <CheckBox Grid.Row="4"
                x:Name="ThinkingCheckBox"
                Content="{x:Static i18n:Strings.PromptEdit_AllowThinking}"
                FontSize="{DynamicResource ByhFontSizeBody}"
                IsChecked="False" />

      <!-- R34: per-template toolbar shortcut. Single character; fires while the
           selection toolbar is visible. Blank = no shortcut for this action. -->
      <StackPanel Grid.Row="5" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
        <TextBlock Text="{x:Static i18n:Strings.PromptEdit_ShortcutLabel}"
                   Classes="Muted"
                   FontSize="{DynamicResource ByhFontSizeBody}"
                   VerticalAlignment="Center" />
        <TextBox x:Name="ShortcutInput"
                 Classes="Bordered"
                 MaxLength="1"
                 Width="40"
                 FontSize="{DynamicResource ByhFontSizeBodyLarge}"
                 PlaceholderText="{x:Static i18n:Strings.Common_Placeholder_None}" />
        <TextBlock Text="{x:Static i18n:Strings.PromptEdit_ShortcutHint}"
                   Classes="Secondary"
                   FontSize="{DynamicResource ByhFontSizeBodySmall}"
                   VerticalAlignment="Center"
                   TextWrapping="Wrap" />
      </StackPanel>

      <Grid Grid.Row="6" ColumnDefinitions="Auto,*,Auto,Auto" ColumnSpacing="8">
        <Button Grid.Column="0"
                x:Name="ResetButton"
                Classes="DangerQuiet"
                Content="{x:Static i18n:Strings.PromptEdit_Reset}"
                Click="OnResetClick" />
        <Button Grid.Column="2"
                Content="{x:Static i18n:Strings.Common_Cancel}"
                Click="OnCancelClick" />
        <Button Grid.Column="3"
                x:Name="SaveButton"
                Classes="Primary"
                Content="{x:Static i18n:Strings.Common_Save}"
                Click="OnSaveClick" />
      </Grid>
    </Grid>
  </Border>
</Window>
