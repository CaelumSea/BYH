id: TASK-014
requirement: REQ-012
title: 实现并验证金属质感双圆角结构框
status: done
created: '2026-07-20T12:29:44'
updated: '2026-07-20T12:50:08'
satisfies:
- AC-1
- AC-2
- AC-3
- AC-4
description: 在 IvoryJade 主题中建立可复用 MetallicFrame 样式，统一设置页结构性外框，保留内部卡片克制层级；完成视觉 QA、测试与
  NativeAOT 发布。
handoff: '复用 Ivory Jade 现有材质 token，新增 ByhMetallicEdgeBrush 与 MetallicFrame/Compact；

  设置页五个结构窗格已统一，FlatRail 与内部卡片保持原层级。默认/最小 175% DPI

  NativeAOT 截图均通过。后续若调整金属强度，只改主题资源，不要恢复多 class 覆盖。

  '
execution_log:
- step: 1
  action: 读取四张参考局部图、Git 工作流、当前 handoff 与主题架构，拆解金属边缘的光学层次。
  result: 确定外层渐变古金、2 DIP 象牙缝、3 DIP 处浅金内线和暖色下投影。
- step: 2
  action: 调用 OMP xiaomi-mimo/mimo-v2.5-pro 只读盘点现有 Border 样式和应用位置。
  result: 确认 DecorativeFrame 与 SettingsFrame 属性互相覆盖；五个结构窗格适合统一样式。
- step: 3
  action: 实现 ByhMetallicEdgeBrush、MetallicFrame 与 Compact，并替换 SettingsWindow 结构框 class。
  artifact: src/SelectionAssistant.UI/Themes/IvoryJade.axaml
- step: 4
  action: 完成 Release build、完整测试与 Windows win-x64 NativeAOT 发布。
  command: dotnet test SelectionAssistant.slnx -c Release --no-build --no-restore;
    dotnet publish src/SelectionAssistant.App/SelectionAssistant.App.csproj -c Release
    -r win-x64 -o artifacts/publish/win-x64-nativeuia
  result: Build 0 warnings/0 errors; tests 232/232; NativeAOT 0 warnings, PDB 0。
- step: 5
  action: 在 175% DPI 下对 NativeAOT 默认与 1240x680 logical 最小窗口逐像素视觉复核。
  artifact: artifacts/qa/ivory-jade-settings-v10-metallic-default-nativeaot.png
- step: 6
  action: 同步主题架构、验证报告与当前 handoff。
  artifact: output/TASK-014-metallic-frame-verification.md
