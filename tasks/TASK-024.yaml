id: TASK-024
requirement: REQ-022
title: 均衡左栏分段并拉高手机面板
status: done
created: '2026-07-22T19:55:00'
updated: '2026-07-22T20:04:44'
satisfies:
- AC-1
- AC-2
- AC-3
- AC-4
description: 压缩左栏上半部多余留白，将空间让给 Typography 与 Icon Style；右上手机改为仅比相邻主设置面板稍矮，并继续保持垂直居中。
handoff: '已完成 v7。左侧 FlatRail 行比例由 1.1/1.65/0.85/0.8 调整为 0.95/1.55/0.9/1.0，并同步压缩 Theme
  Concept 内边距与间距，使上方留白减少、Typography 与 Icon Style 更舒展。手机由 MaxHeight=390 + Center 改为
  MaxHeight=480 + Stretch + 上下 10 margin，默认和最小尺寸都只比主面板稍矮。首次最小尺寸截图发现 Vision OCR 型号被
  dock 遮挡，随后将手机 header 从 148 缩为 136 并重新完成全套验证。证据见 output/TASK-024-verification.md。


  [自检修复] [detail_polish] 最小窗口首轮截图中 Vision OCR 型号被底部 dock 遮挡；将手机 header 从 148 缩为 136
  后重新构建、发布并截图，最终内容完整。'
execution_log:
- step: 1
  action: 对照 v6 默认/最小截图与用户参考图，重新计算左栏四段比例和手机相对高度。
  result: 采用 0.95/1.55/0.9/1.0 左栏比例；手机采用 Stretch + 对称 10 margin。
- step: 2
  action: 修改 SettingsWindow.axaml 的左栏 RowDefinitions、Theme Concept 内间距和 PhoneFrame
    布局参数。
  artifact: src/SelectionAssistant.UI/Views/SettingsWindow.axaml
- step: 3
  action: 执行 XML parse、git diff --check、Release build、334 项测试与 Windows NativeAOT publish。
  result: XML 与空白检查通过；Build 0 warning/0 error；334 passed；NativeAOT 成功。
- step: 4
  action: 截取默认与最小窗口进行视觉检查。
  artifact: artifacts/qa/req-022-v7-balanced-proportions/general-minimum-nativeaot.png
  result: 首轮发现最小窗口 Vision OCR 型号被 dock 遮挡，缩短 header 后重新构建、发布、截图，最终完整可见。
- step: 5
  action: 复核最终截图的相对边界和内容完整性。
  result: 默认尺寸手机相对主面板上/下各缩约 20/17 物理像素；最小尺寸约 17/17；左栏底部两段高度增加且无裁切。
